# Firebase Configuration
FIREBASE_PROJECT_ID=cloudrun-test-dd011

# Note: For local development, you need to set up Firebase Authentication
# Option 1: Use real Firebase project (recommended for testing auth flow)
#   - Create a Firebase project at https://console.firebase.google.com/
#   - Enable Google Sign-in in Authentication > Sign-in method
#   - Set FIREBASE_PROJECT_ID to your project ID (cloudrun-test-dd011)
#   - The app will use Application Default Credentials (run: gcloud auth application-default login)
#
# Option 2: Use Firebase Emulator (for development without real auth)
#   - Install Firebase CLI: npm install -g firebase-tools
#   - Run: firebase emulators:start --only auth
#   - Set FIREBASE_AUTH_EMULATOR_HOST=localhost:9099
