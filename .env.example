# Backend Configuration (for docker-compose.yml)
FIREBASE_PROJECT_ID=cloudrun-test-dd011

# Note: This project requires Firebase Authentication to be set up
# 1. Go to https://console.firebase.google.com/
# 2. Select your project: cloudrun-test-dd011
# 3. Enable Google Sign-in in Authentication > Sign-in method
# 4. Run: gcloud auth application-default login (to set up local credentials)
